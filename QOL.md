Quality of Life

1. The ability to export all timer data to a structured file. Probably JSON.
2. When opening the timer, check if the current date is different than the date of the last session. If so, start a new session automatically for the user.
3. Change session names to just "1" or "2", not "Session N". But generate an internal UUID that can be used for deduplicating externally.
4. Faintly highlight fastest solve for a given session in blue. Faintly highlight fastest time for each phase across a given session in blue.
5. Make viewing the debug log toggleable.
6. Assume the cube state is solved when a cube is first connected.
