# Vendored ESM tree for cubing.js + random-uint-below. Generated by
# scripts/vendor-cubing.mjs before each build (see package.json). Kept
# out of git because it's a verbatim copy of node_modules content.
static/vendor/
